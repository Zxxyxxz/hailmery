node_modules/
.wrangler/
dist/
.env
.env.local
.env.*.local
.dev.vars
*.log
.DS_Store
drizzle/meta/_journal.json.bak
.idea/
.vscode/

# V0 artifacts not for git
screenshot-apire.js
apire-screenshots/

# Source corpus binaries (large .docx/.pdf — extracted text only is in markdown files)
corpus/apire/*.docx
corpus/apire/*.pdf

# out/ is committed for V0 so the user can review generated drafts.
# After V1 we will re-ignore this.
